export interface person {
  id: number;
  firstName: string;
  lastName: string;
  lastSeen: string;
  province: string;
  stopHour: string;
  stopDate: string;
  lastReport: string;
  verification: string;
}

export interface cardTranslations {
  province: string;
  lastSeen: string;
  stopHour: string;
  stopDate: string;
  lastReport: string;
  verification: string;
}
